# Copy to .env (same folder as docker-compose.yml) and fill in. .env is git-ignored -- never commit it.
# Use long random values; these are not the local-development credentials in backend/.env.
MYSQL_ROOT_PASSWORD=
DB_PASSWORD=
# Optional: enables the chatbots' open-ended answers. Leave empty to run without it.
GEMINI_API_KEY=
