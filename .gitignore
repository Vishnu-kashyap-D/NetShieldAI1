artifacts_smoke/
artifacts_category_smoke/
# Transient pre-retrain safety copy (backend/app/routers/retrain.py's quality gate) -- never
# meant to be committed, just a same-machine rollback target for a rejected retrain.
artifacts_backup_before_retrain/
reports_smoke/
reports_category_smoke/
MachineLearningCVE/
__pycache__/
*.pyc
.venv/
venv/
backend/.env
backend/*.log
node_modules/
# Default example --watch-dir location for backend/scripts/live_capture_feed.py -- captured
# pcaps and their converted CSVs are local test data, not something to commit.
captures/

# Throwaway venv for pip-audit (see backend/README.md "Dependency auditing")
.audit-venv/
